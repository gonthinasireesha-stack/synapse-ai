// src/App.jsx
//
// WHY THIS FILE IS MINIMAL RIGHT NOW:
// This is Phase 0 — we're only proving the build/dev pipeline works end to
// end. Real routing (React Router, ProtectedRoute, AuthContext) gets built
// in Phase 1 alongside authentication. This placeholder will be replaced
// then — committing it now would be premature.

function App() {
  return (
    <div style={{ padding: '2rem', fontFamily: 'system-ui' }}>
      <h1>Synapse AI</h1>
      <p>Phase 0: project scaffold is running correctly.</p>
    </div>
  );
}

export default App;